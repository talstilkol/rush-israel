# RUSH — תוכנית ביצוע ממוספרת (קודקס)

**מצב:** תכנון בלבד. אסור לתכנת עד שהמסמך הזה שלם ואושר.  
**יעד ריאלי:** Asphalt-like בדפדפן. לא GT7, לא Unreal, לא GIS.  
**אחוז נוכחי:** ~12%. שערי שחרור: 0/13.  
**מקורות API:** [WebGPURenderer](https://threejs.org/docs/pages/WebGPURenderer.html) · [WebGPU post](https://threejs.org/manual/en/webgpu-postprocessing.html) · [TSL](https://threejs.org/docs/TSL.html) · [KTX2](https://registry.khronos.org/glTF/) · [TAARenderPass — אין reprojection](https://threejs.org/docs/pages/TAARenderPass.html)

מספור: `שלב.תת-שלב.משימה.תת-משימה.פרט`. מספרים בלבד.

---

## 0. חוקים (לא משנים באמצע סשן)

0.1. אסור לטעון GIS / OSM / DEM / "רחובות אמיתיים" / פוטוגרמטריה / רכב סרוק / KTX2-בשידור / WebGPU-כברירת-מחדל עד שהשער של אותו פריט ירוק.  
0.2. אסור להוסיף ערים או מסלולים. אסור לאפות PNG פרוצדורלי חדש.  
0.3. אסור לגעת ב-Phase 7 (ישראל חוץ מאיילון) עד ש-6.5 (הקפאת איילון) ירוק.  
0.4. אסור Unreal / Unity / Nanite / Lumen / RT.  
0.5. אסור אונליין, גאראז׳ שדרוגים, 50 מכוניות.  
0.6. V רק מול מפרט זעיר כתוב מראש + בדיקה שרצה. אחרת PARTIAL/FAKED.  
0.7. כל סשן מימוש (רק אחרי אישור המסמך) עושה **מספר אחד** מהעץ, לא שלושה.  
0.8. קבצי אמת: `src/game/engine.ts`, `world.ts`, `vehicle.ts`, `RendererFacade.ts`. לא `progress.md`.

---

## 1. יושרה, עותק, QA hook, הקלטות

### 1.1. עותק מסלולים — "בהשראת, לא GIS"
1.1.1. לאתר כל מחרוזת תיאור ב-`tracks.ts` / `i18n.ts` / כרטיסי UI.  
1.1.1.1. grep: `כביש 20`, `GPS`, `מדויק`, `רחובות אמיתיים`, `OpenStreetMap`, `סריקה`.  
1.1.1.2. לכל כרטיס: שורה אחת קבועה בעברית ובאנגלית: "בהשראת המקום. לא מפה, לא GIS."  
1.1.1.3. שמות סמלים (עזריאלי, רידינג) מותרים. טענת מיקום מדויק אסורה.  
1.1.2. README: "אב-טיפוס Three.js WebGL. לא סימולטור מדידה."  
1.1.3. שער: צילום מסך של כרטיס איילון + כרטיס ירושלים מציג את המשפט. בלי זה 1.1 לא ירוק.

### 1.2. Hook של `?qa=1`
1.2.1. היום: `qaHookAllowed()` = `VITE_QA===1` או DEV+localhost. `finishNow` חי.  
1.2.2. יעד פרוד: אין `__controlsTest` אלא אם `import.meta.env.VITE_QA==="1"` **בבילד**.  
1.2.2.1. Vite: `VITE_QA` לא מוגדר בבילד פרוד.  
1.2.2.2. `?qa=1` לבד **לא** פותח hook בפרוד.  
1.2.2.3. בדיקה: `npm run build` + grep ל-`finishNow` ב-bundle — חייב להיות מת מתחת ל-`if(false)` או לא קיים.  
1.2.3. DEV מקומי: hook נשאר בשביל `qa:drive`.  
1.2.4. שער: סקריפט `check-qa-hook.mjs` נכשל אם bundle פרוד מכיל `finishNow`.

### 1.3. שיאים
1.3.1. היום: טווח זמן + eligible. אין hash.  
1.3.2. מבנה: `{ t, trackId, carId, physicsVersion, hash }`.  
1.3.2.1. `physicsVersion` קבוע ב-`vehicle.ts` (למשל `3`). כל שינוי Pacejka/מסה מעלים גרסה.  
1.3.2.2. hash = SHA-256 של `trackId|carId|t|physicsVersion` (בלי מפתח סודי — זה לא אנטי-צ'יט, זה דחיית גרסאות).  
1.3.2.3. טעינה: אם `physicsVersion` ≠ נוכחי → לא מציגים שיא.  
1.3.3. לא לחתום עם HMAC בלי שרת. אל תטען "שיא רשמי".  
1.3.4. שער: טסט יחידה: שיא עם גרסה ישנה נדחה.

### 1.4. `@ts-nocheck`
1.4.1. היום: `world.ts`, `game-app.tsx`, `routeTree.gen.ts` (גנרט — לא לגעת).  
1.4.2. כלל: קובץ חדש בלי nocheck.  
1.4.3. פיצול `world.ts` (אחרי freeze איילון, לא לפני):  
1.4.3.1. `world/buildRoad.ts`  
1.4.3.2. `world/ayalon.ts`  
1.4.3.3. `world/israelLandmarks.ts`  
1.4.3.4. `world.ts` רק `createWorld()`.  
1.4.4. `game-app.tsx`: להמיר בהדרגה ל-TSX טיפוסי. לא בסשן freeze.  
1.4.5. שער: `tsc --noEmit` בלי nocheck על קבצים חדשים. `world.ts` נשאר nocheck עד 1.4.3.

---

## 2. תקציב פריים (G0) — Codex §25. לא WebGPU

### 2.1. טלמטריה
2.1.1. היום: `RenderTelemetry` p50/p95/p99, HUD p95 ב-`?qa=1`.  
2.1.2. להוסיף (בלי קנבס חדש):  
2.1.2.1. `renderer.info.render.calls`  
2.1.2.2. `renderer.info.render.triangles`  
2.1.2.3. `renderer.info.memory.geometries` / `textures`  
2.1.3. HUD `?qa=1`: שורה `webgl2 · p95 14ms · dc 48 · tri 120k`.  
2.1.4. אין GPU timer query ב-WebGL2 כברירת מחדל (הרשאות). לא לטעון "GPU ms" בלי `EXT_disjoint_timer_query_webgl2` + מדידה אמיתית.  
2.1.5. שער: צילום HUD באיילון High.

### 2.2. הורדת איכות אוטומטית
2.2.1. היום: `DynamicQualityController` מוריד composer. **לא** את הסדר של קודקס.  
2.2.2. סדר חובה כש-p95 > 20ms במשך 90 פריימים:  
2.2.2.1. כיבוי planar reflector  
2.2.2.2. כיבוי bloom  
2.2.2.3. כיבוי CSM (חזרה לצל אחד בלי מפה / blob בלבד)  
2.2.2.4. `pixelScale *= 0.85` עד מינימום 0.5  
2.2.3. עלייה חזרה רק אחרי 5 שניות מתחת ל-16ms. היסטרזיס חובה.  
2.2.4. Low: composer כבוי (כבר). לא לגעת.  
2.2.5. שער: לוג DEV שמדפיס איזה מעבר נכבה.

### 2.3. יעד 60Hz
2.3.1. תקציב 16.6ms: גיאומטריה 7 · צל 2 · planar 1.5 · פוסט 1.5 · JS פיזיקה 3 · רזרבה 1.6.  
2.3.2. לא ניתן לאמת iPhone בסנדבוקס. שער זמני: Playwright desktop p95 < 20ms באיילון High, viewport 1280×800.  
2.3.3. שער טלפון: ידני אצל המשתמש. לא לסמן 100 בלי זה.

### 2.4. CSM maps ב-ResourceRegistry
2.4.1. היום: env/probe/composer מוחכרים. מפות הצל של `three/examples/jsm/csm/CSM.js` פנימיות.  
2.4.2. מימוש: לא לפרוץ ל-CSM. ב-`dispose()` לקרוא `csm.dispose()` אם קיים, ואז `registry.dispose()`.  
2.4.3. לא לעטוף `WebGLRenderTarget` פנימי של CSM — זה שביר בין גרסאות three.  
2.4.4. שער: 20 כניסות/יציאות מרוץ בלי עלייה ב-`renderer.info.memory.textures`.

---

## 3. כביש (P0 + G1) — התמונה החשובה ביותר אחרי הרכב

### 3.1. עובדות API (מחקר)
3.1.1. `MeshPhysicalMaterial.onBeforeCompile` **מתנגש** עם `CSM.setupMaterial` — CSM דורס `onBeforeCompile`.  
3.1.1.1. חובה לשרשר: לשמור את ה-compile של CSM ואז להזריק ליינים. או: ליינים כ-mesh (היום) ולא שיידר.  
3.1.1.2. יעד השיידר: **אחרי** שרשור CSM, לא במקומו.  
3.1.2. TSL (`MeshPhysicalNodeMaterial`) **לא** רץ על `WebGLRenderer` (three#28957). שיידר כביש ב-TSL = רק נתיב WebGPU בשלב 8.  
3.1.3. לכן שלב 3 = GLSL/`onBeforeCompile` על WebGL. שלב 8 = שכפול TSL.

### 3.2. UV של הסרט
3.2.1. `buildRoad`: `uv.x` = 0 בשוליים שמאליים, 1 בימניים (רוחב מלא). `uv.y` = `s / laneMarkPeriod` (מטר לאורך).  
3.2.2. `laneCount` uniform: איילון 8, כביש מהיר 4, עירוני 3.  
3.2.3. רוחב נתיב: `built.width / lanes`. איילון: 28 / 8 = 3.5 בדיוק. **לא** לשנות את 28.  
3.2.4. נתיב 4/8 באיילון (האמצע) = לא דאש לבן (יש ג'רזי בין הכבישים; על המסלול של השחקן האמצע הוא בין 4 ל-5). דאש לבן ב-`ln = 1..lanes-1` חוץ מ-`lanes/2` כש-lanes זוגי ורחב.  
3.2.5. שוליים: קו לבן רציף ב-`uv.x≈0` ו-`≈1` ברוחב 0.012 במרחב UV.  
3.2.6. קו צהוב דק ליד הלבן (כבר mesh — אפשר להשאיר mesh כדי לא להילחם ב-CSM).

### 3.3. חומר יבש/רטוב
3.3.1. יבש: `metalness=0` (חובה, dielectric). `roughness≈0.62`. `clearcoat≈0.12`. `envMapIntensity≈0.32`.  
3.3.2. רטוב: `roughness→0.18`, `clearcoat→0.55`, `clearcoatRoughness→0.12`, planar חז יותר.  
3.3.3. מטר גשם קיים: לערבב לפי `weather`, לא טקסטורת canvas.  
3.3.4. אספלט: נשאר `asphalt-8.png` וכו׳. לא לאפות חדש. השיידר **מוסיף** דאשים מעל המפה.

### 3.4. Planar
3.4.1. היום: `Reflector` 768, yaw לפי הרכב, clip.  
3.4.2. `Reflector` של three הוא WebGL RT. **לא** עובד ב-WebGPURenderer.  
3.4.3. חוקים WebGL:  
3.4.3.1. High + לילה בלבד. Mid/Low: כבוי.  
3.4.3.2. כיבוי אוטומטי ב-2.2.2.1.  
3.4.3.3. לא להעלות מעל 768.  
3.4.4. WebGPU (שלב 8): planar נדחה או cubemap probe בלבד. אל תבטיח SSR.

### 3.5. נגד + מסילה באיילון
3.5.1. היום: `buildOffsetRoad` בהיסט `width+18`, ג'רזי, פסי רכבת. ויזואלי.  
3.5.2. אסור ספליין שני שניתן לנהוג בו עד שיש חיבור מחלף אמיתי.  
3.5.3. רכבת: משטח נע על ציר Z של האמצע. לא פיזיקה.  
3.5.4. שער: השחקן לא יכול "לקפוץ" לנתיב הנגדי בלי רמפה.

### 3.6. canvas NYC מחוץ ל-bundle ישראל
3.6.1. היום: `world.ts` מייבא סטטית `nyc-canvas.ts`.  
3.6.2. יעד: `if (def.city==="nyc") { const m = await import("./nyc-canvas"); }` מתוך `createWorld` האסינכרוני, או `addNycLandmarks` כבר אסינכרוני.  
3.6.3. שער: `grep` על chunk הראשי אחרי build — `adBoardTexture` לא ב-index.

---

## 4. רכב (Codex §25.1)

### 4.1. מה אסור לטעון
4.1.1. חמשת ה-`.glb` הם extrusion שיוצא ב-`bake-car.mjs`. לא סריקה, לא GT7.  
4.1.2. `KTX2Loader` מחובר אבל **0 קבצים** — לא "פייפליין KTX2".

### 4.2. רכב hero אחד
4.2.1. מקור חיצוני CC0/CC-BY עם רישיון בקובץ `public/game/LICENSES.md`.  
4.2.2. דרישות mesh:  
4.2.2.1. גוף אחד בשם `body`  
4.2.2.2. 4 גלגלים `wheel_fl/fr/rl/rr` עם pivot במרכז  
4.2.2.3. זכוכית נפרדת `glass`  
4.2.2.4. < 40k משולשים ל-LOD0, LOD1 12k, LOD2 4k  
4.2.3. אם אין נכס חוקי בסנדבוקס: **לא** לייצר extrusion נוסף. 4.2 נשאר NOT DONE.  
4.2.4. שער: צילום chase מול צילום ייחוס של אותו דגם (או הודאה שאין).

### 4.3. צבע flakes
4.3.1. היום: `flake.png` כ-roughness/bump/clearcoatNormal.  
4.3.2. יעד WebGL: `onBeforeCompile` — `clearcoatNormal` += רעש תלת-צירי קטן לפי `worldPosition`. לא canvas.  
4.3.3. יעד WebGPU: TSL `normalMap` + `mx_noise_float` / hash. רק אחרי 8.2.  
4.3.4. `metalness=0` לצבע מכונית dielectric (לא כרום). כרום רק על trim.  
4.3.5. שער: צילום שמש על מכסה המנוע — נצנוץ לא מפת bump שטוחה.

### 4.4. מצלמת hood
4.4.1. היום: קופסאות דאש תמיד דולקות.  
4.4.2. hood: להחביא `windshield` (`visible=false` על `glass` הקדמי אם קיים).  
4.4.3. chase: דאש `visible=false`.  
4.4.4. אין תא נהג מלא בלי mesh חיצוני. לא לפסל מושבים.

### 4.5. טלמטריית תערובת
4.5.1. היום: `yawRate = kin * crawl + tire`.  
4.5.2. HUD `?qa=1`: `kinMix = crawl` (0 במהירות, 1 בעמידה).  
4.5.3. אל תסתיר את המספר.

### 4.6. 0–100
4.6.1. פרוטוקול: איילון, ישר, TCS כבוי, 5 דגמים, ממוצע 3 ריצות.  
4.6.2. סובלנות מול מספר מפורסם לדגם: ±15% אחרי hero; היום ±30% לא שער.  
4.6.3. CI: Playwright `qa:accel` כותב JSON. לא לסמן 100 בלי קובץ תוצאות ב-repo.

---

## 5. פיזיקה

### 5.1. יאו
5.1.1. היום: Pacejka לרוחב + קינטי בזחילה.  
5.1.2. לא להרוג קינטי מתחת ל-~10 מ׳/ש׳ — Pacejka מת מוסר.  
5.1.3. אופציה א: להשאיר ולכתוב ב-HUD "simcade".  
5.1.4. אופציה ב: מודל אופניים במהירות נמוכה (kinematic bicycle) במקום `kin*1.7`. עדיין לא 4 גלגלים טהורים.  
5.1.5. אסור לקרוא "tire-only" כל עוד `crawl>0` משפיע.  
5.1.6. שער: לוג `crawl` ב-0 כש-speed>12.

### 5.2. מתלים
5.2.1. היום: קפיץ Y מול `sample.y` של הספליין.  
5.2.2. יעד 4-post: 4 קפיצים מול גובה קרקע מקומי (ספליין + רמפה), לא 6DoF.  
5.2.3. בלי DEM. `groundY = splineY + rampY`.  
5.2.4. travel ±0.12m. compression משפיע על grip (load transfer פשטני).  
5.2.5. שער: על רמפת השלום הרכב עולה ב-Y בלי לקפוץ ל-splineY.

### 5.3. שיפוע
5.3.1. `grade * g` מנגד הספליין.  
5.3.2. אסור המילה DEM בקוד/UI.  
5.3.3. ירושלים/רמון/חרמון: רק גבהי ספליין.

### 5.4. OBB
5.4.1. היום: yaw בחלק מהמנהרות/שערים. רוב `hit(x,z,r)` עיגול.  
5.4.2. כל סמל עם yaw: `{x,z,hx,hz,yaw,kind}`.  
5.4.3. בניין = עצירה. מעקה = האטה. רכב = דחיפה.  
5.4.4. עמוד מחלף: מחוץ ל-`width/2+2` (כבר הוזז).  
5.4.5. שער: מעבר איילון 200מ׳ בלי פגיעת עמוד.

### 5.5. CCD
5.5.1. Sweep קצר 12ms או 2 substeps כש-speed>25.  
5.5.2. לא PhysX.

---

## 6. הקפאת איילון — שער 1 (חוסם הכל אחריו)

### 6.1. צילום ייחוס השלום — **תלוי במשתמש**
6.1.1. בלי תמונת ייחוס מהמשתמש (או URL מוסכם) **אסור** לסמן 6.1.  
6.1.2. פרוטוקול QA:  
6.1.2.1. מצלמת משחק: אותו yaw כמו נתיב דרום→צפון, Hashalom GPS `32.0735, 34.796`.  
6.1.2.2. צילום `golden-baseline/ayalon-hashalom.png`.  
6.1.2.3. רשימת ויזואלית: עזריאלי משולש+עיגול+ריבוע משמאל/ימין נכון, גשר השלום מעל, נגד 8 נתיבים, מסילה באמצע.  
6.1.2.4. כישלון אם: קוביות גנריות, נגד חסר, עזריאלי באותו גובה, אופק חתוך.  
6.1.3. סוכן לא "מאשר" דמיון. המשתמש כותב "hashalom ok" או שולח צילום נגד.

### 6.2. עזריאלי — מספרים (ויקיפדיה / Azrieli Group)
6.2.1. עיגול: **187m**, 49 קומות.  
6.2.2. משולש: **169m**, ~45–46 קומות.  
6.2.3. ריבוע: **154m**, ~41–42 קומות.  
6.2.4. בקוד: `1 יחידה = 1 מטר`. גבהים 187 / 169 / 154 לא יחסיים מזויפים.  
6.2.5. זכוכית: `MeshPhysicalMaterial` dielectric-ish גג+פאנלים. **לא** `BoxGeometry` אחד לכל מגדל בלי פסים אופקיים כל ~3.8m (גובה קומה).  
6.2.6. אסור תמונת חזית מ-canvas. אם אין טקסטורת חלון חוקית: פסים גיאומטריים + darkGlass.  
6.2.7. מיקום: GPS קיים; היסט `width/2+offset` כדי לא לחסום נתיב.

### 6.3. ToHa / City Gate / Midtown / סבידור
6.3.1. כל אחד: צללית שונה (גובה, יחס, חומר). אסור שני Box זהים.  
6.3.2. סבידור: אולם + רציף ארוך יותר (כבר `kind==="savidor"`). לא להעתיק את השלום.  
6.3.3. לא פוטוגרמטריה.

### 6.4. מחלפים
6.4.1. שישה שמות: גלויות, הגנה, לה גרדיה, השלום, סבידור, אוניברסיטה.  
6.4.2. דק מעל הכביש ב-Y≈9.4, עמודים מחוץ לנתיב (כבר).  
6.4.3. `pushRamp` חייב להתחבר לדק. השחקן שעולה רמפה מקבל `probeRamp` Y.  
6.4.4. אם רמפה לא מחוברת — למחוק את הרמפה, לא להשאיר גשר יתום.  
6.4.5. שער: סקריפט שמציב את הרכב על רמפת השלום ומוודא `y>4` אחרי 2 שניות תאוצה.

### 6.5. הקפאה
6.5.1. אחרי 6.1 אושר: קובץ `src/game/world-ayalon.ts` (או בלוק) לא נערך בלי bump ל-`AYALON_ART_REV`.  
6.5.2. CI: hash של קובץ האיילון. שינוי בלי bump נכשל.  
6.5.3. עד 6.1 ירוק — מותר תיקוני חסימה בלבד, לא "עוד צללית".

---

## 7. ישראל אחרי 6.5 בלבד

סדר קשיח. לכל מסלול אותו חוזה 7.0.

### 7.0. חוזה מסלול
7.0.1. 8 סמלים עם lat/lon ידני + מקור (ויקיפדיה).  
7.0.2. כל סמל: היסט מחוץ ל-`width/2+8` או מנהרה/גשר עם OBB.  
7.0.3. אפס `scatterStreetBuildings`.  
7.0.4. צילום chase + רשימת "נראה כמו המקום" מהמשתמש.  
7.0.5. בלי זה המסלול נשאר PARTIAL.

### 7.1. רוטשילד
7.1.1. חצי אי תנועה + פיקוס (יש). LOD: icosahedron רחוק, 10 כתרים קרוב (יש צפיפות).  
7.1.2. היכל העצמאות: צללית באוהאוס, לא גורד.  
7.1.3. לא חזיתות canvas.

### 7.2. חוף / רידינג
7.2.1. מנהרה עבירה (יש). ארובה מחוץ לכביש (יש).  
7.2.2. הילטון טרסות (יש).  
7.2.3. ים: גל plane + foam PNG קיים. לא אוקיינוס וולומטרי.

### 7.3. יפו
7.3.1. שעון מוסט. כורכר/קשתות.  
7.3.2. אסור בניין מודרני גנרי על הספליין.

### 7.4. ירושלים A→B
7.4.1. שער יפו מחוץ לכביש. אבן. גבעות ספליין. ערפל דליל.  
7.4.2. אסור DEM. אסור רקע חתוך (far plane / fog).

### 7.5. רמון A→B
7.5.1. אפס בניינים. מצוק. שיפוע.  
7.5.2. אם יש Box עירוני — באג, למחוק.

### 7.6. חרמון A→B
7.6.1. קו שלג לפי Y. עלייה. לא לולאה.

### 7.7. כרמל
7.7.1. בהאאי מוסט (יש). יער. ירידה. לא מקדש על האספלט.

---

## 8. Renderer — WebGPU רק אחרי 3+6

### 8.1. ממצאי מחקר (חובה לקרוא לפני מימוש)
8.1.1. `WebGPURenderer` מ-`three/webgpu`. ברירת מחדל: WebGPU, נפילה ל-WebGL2. `forceWebGL: true` ל-CI.  
8.1.2. חלק מהמדריכים דורשים `await renderer.init()`. לבדוק את גרסת `three` ב-`package.json` לפני כתיבה.  
8.1.3. TSL / NodeMaterial **רק** על `WebGPURenderer`.  
8.1.4. `EffectComposer` (WebGL) **לא** עובד עם `WebGPURenderer`. פוסט = `pass()` / TSL מ-`three/tsl` + addons.  
8.1.5. יש `traa` אמיתי ב-`three/addons/tsl/display/TRAANode.js` (velocity). זה **לא** `TAARenderPass`.  
8.1.6. `TAARenderPass` בתיעוד: "uses no reprojection so it is no TRAA". **אסור** לשלוח אותו.  
8.1.7. `CSM.js` = `onBeforeCompile` → **שבור על TSL**. ב-WebGPU: בלי CSM או CSM מותאם (אין רשמי יציב).  
8.1.8. `Reflector` WebGL RT → שבור על WebGPU.  
8.1.9. InstancedMesh כבד ב-WebGPU (UBO לכל instance) — discourse 2025. להעדיף `BatchedMesh` לצמחייה אם עוברים.  
8.1.10. מסקנה: **ברירת המחדל נשארת WebGLRenderer.** WebGPU רק `?webgpu=1` + adapter.

### 8.2. נתיב כפול
8.2.1. `RendererFacade.init`:  
8.2.1.1. אם אין `?webgpu=1` → WebGL כמו היום.  
8.2.1.2. אם יש: `try { const {WebGPURenderer} = await import("three/webgpu"); ... } catch → WebGL`.  
8.2.1.3. `telem.backend = "webgpu" | "webgl2"`.  
8.2.2. פוסט: WebGL = composer+SMAA. WebGPU = TSL pass + fxaa/traa **רק אם** `velocity` MRT זמין, אחרת בלי AA.  
8.2.3. CSM: WebGPU High בלי CSM עד שיש פתרון. לא לשבור צללים.  
8.2.4. שער: `?webgpu=1` על כרום עם GPU, ו-`forceWebGL` CI ירוק.

### 8.3. TSL כביש+רכב
8.3.1. רק אחרי 8.2 ירוק ו-3.3 קיים ב-GLSL.  
8.3.2. לשכפל התנהגות, לא "שיפור".  
8.3.3. שער: צילום WebGL מול WebGPU אותו pose, diff < סף (לא זהות פיקסל).

### 8.4. CSM 3/1/0 על WebGL
8.4.1. High: 3 cascades @1024 או 2@1024 אם p95>18.  
8.4.2. Mid: 1 cascade @512.  
8.4.3. Low: 0. blob בלבד.  
8.4.4. `setupMaterial` אחרי יצירת world+car, כולל שרשור 3.1.1.

### 8.5. SMAA / TRAA
8.5.1. WebGL High/Mid: SMAA (יש).  
8.5.2. אסור `TAARenderPass`.  
8.5.3. TRAA: רק WebGPU + `traa` מ-addons + velocity. לא לפני 8.2.

### 8.6. SSGI / וולומטרי / מזג אוויר כבד
8.6.1. **אסור** עד hero (4.2) + freeze איילון (6.5).  
8.6.2. 7.5 נשאר DONE כאילוץ.

### 8.7. שכבות קודקס 1.1
8.7.1. Ultra WebGPU = 8.2+8.3+8.5.3 אחרי תקציב.  
8.7.2. Balanced = WebGPU בלי TRAA / עם fxaa, או WebGL High.  
8.7.3. Compat = WebGL Low.  
8.7.4. Photo = אותו renderer, DPR↑, 30fps מותר, לא נכס אחר.

---

## 9. נכסים (E)

### 9.1. KTX2
9.1.1. `KTX2Loader` תומך Basis UASTC/ETC1S בלבד, לא ASTC גולמי.  
9.1.2. יצירה: `gltf-transform` + `toktx` / Basis. **אין KTX-Software בסנדבוקס כרגע.**  
9.1.3. החלטה:  
9.1.3.1. אם אפשר להתקין `gltf-transform`+basis בלי root — לדחוס רק מפות אספלט+flake.  
9.1.3.2. אחרת: **להסיר** `setKTX2Loader` כדי לא לשקר, או להשאיר loader מת + הערה.  
9.1.4. שער: קובץ `.ktx2` ב-`public/game` נטען בלי שגיאה, **או** ה-loader הוסר.

### 9.2. אפס canvas
9.2.1. מותר: `CapabilityProbe` (קנבס WebGL2, לא טקסטורה).  
9.2.2. לאסור: `nyc-canvas.ts`, LED ב-`nyc-landmarks.ts`.  
9.2.3. NYC: PNG באטלס או לא NYC בבילד ישראל.  
9.2.4. Probe RT / env RT = WebGL RT, לא canvas 2D — מותר.

### 9.3. Meshopt
9.3.1. DONE זעיר. לא לשבור.  
9.3.2. רכב חדש ב-4.2 גם Meshopt.

### 9.4. World cells
9.4.1. רשת 256m. סמלי דרך ב-groups לפי תא.  
9.4.2. add/remove לפי תא השחקן ±1.  
9.4.3. לא לפני freeze איילון (העולם קטן מספיק).  
9.4.4. InstancedMesh לצמחייה נשאר; BatchedMesh רק בנתיב WebGPU אם 8.1.9 כואב.

---

## 10. שחרור (G) — 13 שערים

10.1. `qa:drive` ירוק (יש, חלקי).  
10.2. `qa:webgl2` ירוק (יש, חלקי).  
10.3. Pixel-diff: g01/g07/g08 מול PNG, סף (למשל 5% pixels > 16/255). בלי זה אין "golden CI".  
10.4. WebGPU CI רק אם 8.2: `forceWebGL` + אופציונלי skip אם אין adapter.  
10.5. Hook לא בפרוד (1.2).  
10.6. איילון still אושר משתמש (6.1).  
10.7. 5 דגמים 0–100 בקובץ (4.6) — אחרי hero או במפורש "extrusion ±30%".  
10.8. אפס canvas 2D חוץ מ-probe (9.2).  
10.9. p95 איילון High < 20ms ב-Playwright.  
10.10. Esc עובד.  
10.11. עברית + "בהשראת" (1.1).  
10.12. 20× כניסה/יציאה בלי דליפת RT (2.4).  
10.13. אין `@ts-nocheck` בקובץ חדש; `physicsVersion` בשיאים (1.3).

כולם אדומים חוץ מתשתית 10.1/10.2 חלקית.

---

## 11. מחוץ לסנדבוקס (לא לתכנן מימוש כאן)

11.1. Unreal 5.8 + Chaos.  
11.2. OSM / DEM / EPSG:2039.  
11.3. סריקת לייזר לרכב/כביש.  
11.4. Nanite / Lumen / RT.  
11.5. סיבוב סוד OAuth בברוקר.  
11.6. 50 מכוניות, אונליין, גאראז׳.

---

## 12. סדר ביצוע אחרי אישור המסמך (מספר אחד לסשן)

12.1. 1.1 עותק inspired.  
12.2. 1.2 hook פרוד.  
12.3. 4.5 HUD kinMix (זול, מונע שקר פיזיקה).  
12.4. 3.6 dynamic import NYC.  
12.5. 2.1 draw calls ב-HUD.  
12.6. 2.2 סדר כיבוי planar→bloom→CSM.  
12.7. 3.2+3.3 שיידר כביש משורשר ל-CSM.  
12.8. 6.4.5 טסט רמפת השלום.  
12.9. **עצירה ל-6.1** — המשתמש שולח/מאשר צילום השלום.  
12.10. 6.2 פסי קומות עזריאלי לפי 187/169/154.  
12.11. 6.5 הקפאה + hash.  
12.12. רק אז 7.x לפי הסדר.  
12.13. רק אז 8.2 `?webgpu=1`.  
12.14. 9.1 KTX2 או הסרת loader.  
12.15. 10.3 pixel golden.

אם סשן מתחיל ב-7 או 8 לפני 6.5 — זה הפרת 0.3.

---

## 13. שאלות פתוחות — תשובות תכנון (לא לחכות)

13.1. "האם מחליפים ל-WebGPU עכשיו?" **לא.** Composer, CSM, Reflector נשברים.  
13.2. "האם TSL לכביש ב-WebGL?" **לא.** Nodes לא על WebGLRenderer.  
13.3. "האם TAA של three זה TRAA?" **לא.** בלי reprojection.  
13.4. "האם יש TRAA אמיתי?" כן, `TRAANode` ב-WebGPU בלבד.  
13.5. "האם freeze בלי תמונת המשתמש?" **לא.**  
13.6. "האם extrusion = hero?" **לא.**  
13.7. "האם loader KTX2 בלי קבצים = E1?" **לא.**  
13.8. "האם מקפי דרך = שיידר כביש?" **לא.**  
13.9. "האם 60fps iPhone?" לא ניתן לאמת כאן.  
13.10. "האם GIS בשלב מאוחר?" רק מחוץ לסנדבוקס (11.2).

---

**סוף חלק א׳.** חלק ב׳ מתחיל בסעיף 14.

---

## 14. חוזי GPS — 8 סמלים למסלול (ידני, לא OSM)

כל נקודה: lat, lon, מקור, חובה ויזואלית, חובה היסט.  
אלה **נקודות ייחוס לאמנות**, לא מפת כביש. הטענה "GIS" נשארת אסורה (0.1).

### 14.1. איילון (שער 1 — לפני freeze)
14.1.1. עזריאלי עיגול — 32.07455, 34.79195 — גובה 187m, גליל+כתר. מקור: Azrieli Group.  
14.1.2. עזריאלי משולש — 32.07425, 34.79285 — 169m, חתך משולש.  
14.1.3. עזריאלי ריבוע — 32.07485, 34.79145 — 154m, פריזמה.  
14.1.4. גשר/תחנת השלום — 32.0735, 34.79605 — דק מעל הנתיב, רמפות. ויקיפדיה: צמוד למחלף השלום.  
14.1.5. סבידור מרכז — 32.08389, 34.79833 (ויקיפדיה תחנה) / בקוד 32.0837 סביב 34.796. אולם+רציף ארוך, לא העתק השלום.  
14.1.6. ToHa — 32.0714, 34.7866 — מנסרה נטויה, לא גליל. צומת יגאל אלון/השלום.  
14.1.7. Midtown / City Gate — 32.0798–32.0856, 34.793–34.799 — שני גבהים שונים, אסור Box זהה.  
14.1.8. אוניברסיטת ת״א / צפון האיילון — 32.1124, 34.8046 — ספרייה+כיפה נמוכה, לא גורד.  
14.1.9. היסט: כל מגדל `width/2 + ≥16m`. עמודי מחלף `width/2 + 12m` (כבר).  
14.1.10. פסילת QA: שלושת עזריאלי באותו גובה; נגד חסר; עמוד בתוך 14m ממרכז הספליין.

### 14.2. רוטשילד (רק אחרי 6.5)
14.2.1. היכל העצמאות — 32.06278, 34.77083 (ויקיפדיה) / בקוד 32.0629, 34.7695 — בית 2–3 קומות באוהאוס, לא מגדל. כתובת: רוטשילד 16.  
14.2.2. שדרת פיקוס — לא נקודת GPS; כל 15–20m על המדיאן.  
14.2.3. מגדל מאייר/סביבה — 32.0648, 34.7752 — גבוה מהבאוהאוס, לא גנרי זהה.  
14.2.4. Habima — 32.0734, 34.7826 — גג קשת/קליפה, לא קובייה.  
14.2.5. Dizengoff Center — 32.0753, 34.7748 — גשר מעל, לא לחסום שדרה.  
14.2.6. שדרות רוטשילד דרום (אלנבי) — קצה הספליין.  
14.2.7. שדרות רוטשילד צפון (הבימה).  
14.2.8. פינת נחלת בנימין / אלנבי — צללית נמוכה.  
14.2.9. פסילה: `scatterStreetBuildings` דולק; פיקוס בתוך נתיב המכונית.

### 14.3. חוף / רידינג
14.3.1. רידינג — 32.10278, 34.77889 (ויקיפדיה) / בקוד 32.1044, 34.7794 — ארובה+מנהרה עבירה.  
14.3.2. הילטון ת״א — 32.0893, 34.7694 — גליל מדורג טרסות.  
14.3.3. אופרה / רציף — 32.0768, 34.7662.  
14.3.4. דן ת״א — 32.0814, 34.7672.  
14.3.5. מרינה — 32.0938, 34.7688.  
14.3.6. גן צ׳ארלס קלור — 32.0618, 34.7612.  
14.3.7. מגדל הדירות/ים — 32.0795, 34.7668.  
14.3.8. טיילת רציפה — רצועת UV לאורך, לא GPS בודד.  
14.3.9. פסילה: ארובה על האספלט; מנהרה סגורה (OBB חוסם).

### 14.4. יפו
14.4.1. מגדל השעון — 32.0547, 34.7556 — אבן, שעון, לא זכוכית מודרנית.  
14.4.2. כיכר השעון / יפת — 32.0556, 34.7558.  
14.4.3. הנמל — 32.0524, 34.7492.  
14.4.4. כנסיית פטרוס — 32.0546, 34.7508.  
14.4.5. מגדלור — 32.0533, 34.7502.  
14.4.6. שוק הפשפשים — 32.0535, 34.7588 — קשתות אוכרה, לא קניון.  
14.4.7. אנדרומדה — 32.0528, 34.7486.  
14.4.8. קדם / סמטאות — 32.0542, 34.752.  
14.4.9. פסילה: חזיתות `manhattan`/`highway` על הספליין.

### 14.5. ירושלים A→B
14.5.1. שער יפו — ~31.7767, 35.2272 — מחוץ לכביש, קשת עות׳מאנית.  
14.5.2. חומות — רצועת אבן במקביל לספליין, לא קוביות.  
14.5.3. מגדל דוד (צללית מרחוק) — ~31.776, 35.228.  
14.5.4. רחוב יפו / ממילא (עירוני נמוך).  
14.5.5. גבעת הר הרצל/כניסה מערבית — גובה ספליין, לא DEM.  
14.5.6. גן סאקר (ירוק, לא בניין).  
14.5.7. כנסת/גבעת רם כרקע רחוק אם בכלל — LOD תיבה אחת, לא עיר גנרית.  
14.5.8. הר הצופים רק במסלול scopus, לא על jerusalem.  
14.5.9. פסילה: fog חותך אופק; שער בתוך הנתיב; אבן בטון אפור ת״א.

### 14.6. רמון A→B
14.6.1. מרכז מבקרים מצפה רמון — ~30.610, 34.802 — קצה עליון, לא באמצע המכתש.  
14.6.2. כביש 40 ירידה — ספליין עם ΔY שלילי גדול.  
14.6.3. מצוק צין / שפת המכתש — ribbon משמאל/ימין.  
14.6.4. קרקעית — חול/סלע, אפס בניינים.  
14.6.5. חניון תצפית.  
14.6.6. מחצבים/חניון תחתון כנקודת סיום.  
14.6.7. שמים בהירים, ערפל דליל לפי גובה.  
14.6.8. אין רכבת, אין עזריאלי, אין כורכר יפו.  
14.6.9. פסילה: `BoxGeometry` עירוני כלשהו.

### 14.7. חרמון A→B
14.7.1. אתר סקי / חניון תחתון — ~33.31, 35.80.  
14.7.2. כביש עולה, ΔY חיובי.  
14.7.3. קו עצים: מתחת לגובה X ירוק, מעל X שלג.  
14.7.4. מצוק/טרסות סלע.  
14.7.5. תחנת רכבל כצללית, מחוץ לכביש.  
14.7.6. פסגה כרקע, לא יעד חובה.  
14.7.7. אין לולאה סגורה.  
14.7.8. אין בניין ת״א.  
14.7.9. פסילה: שלג בגובה 0; מסלול שטוח.

### 14.8. כרמל
14.8.1. גנים בהאיים — ~32.8118, 34.9853 — תוף+כיפה, היסט ≥58m מהספליין (כבר כיוון).  
14.8.2. מרכז הכרמל / יער.  
14.8.3. ירידה לחיפה, ΔY.  
14.8.4. נמל חיפה כרקע רחוק אופציונלי.  
14.8.5. סטלה מאריס רק אם המסלול הוא stellamaris, לא לכפות על haifa.  
14.8.6. ברושים בצידי כביש, לא icosahedron פיקוס.  
14.8.7. אבן + טיח, לא זכוכית איילון.  
14.8.8. פסילה: מקדש על האספלט.

### 14.9. כלל לכל 14.x
14.9.1. קובץ `src/game/landmark-gps.ts` (כשיותר לתכנת): `Record<trackId, {he,en,lat,lon,minOffset}[]>`.  
14.9.2. טסט: לכל סמל, `nearestIndex` dist מהספליין > `minOffset` או kind=tunnel.  
14.9.3. בלי הקובץ — 7.x נשאר PARTIAL גם אם נראה יפה.

---

## 15. Pixel-diff / Golden (שער 10.3)

### 15.1. מה לא
15.1.1. השוואת JPEG דחוס.  
15.1.2. `TAARenderPass` כ"ייצוב".  
15.1.3. אישור סוכן ("נראה דומה").

### 15.2. כלים
15.2.1. Playwright PNG 1280×800, `deviceScaleFactor=1`.  
15.2.2. `pixelmatch` (npm) על RGB, threshold 0.12.  
15.2.3. כישלון אם `diffRatio > 0.05` (5% פיקסלים) **או** bounding-box של diff על הכביש הקרוב > 8% משטח התחתון של התמונה.  
15.2.4. מסיכת HUD: להתעלם מ-64px עליונים אם `?qa=1` מציג p95.

### 15.3. מצלמות חובה
15.3.1. `g01` איילון יום chase מאחורי הרכב, Hashalom בפריים.  
15.3.2. `g07` איילון יום, עזריאלי ממלאים ≥15% מהמסך.  
15.3.3. `g08` איילון לילה, פנסים על האספלט, בלי שמש לבנה.  
15.3.4. אחרי 6.5: `hashalom` pose קבוע ב-`goldenCameras.ts`.

### 15.4. תהליך
15.4.1. `npm run qa:golden` כותב `tmp/*.png`.  
15.4.2. משווה ל-`golden-baseline/*.png` ב-git.  
15.4.3. עדכון baseline רק עם commit נפרד `golden: bump g01` אחרי אישור משתמש.  
15.4.4. גודל קובץ >20KB (יש היום) = **לא** שער. זה רק smoke נגד קובץ ריק.

### 15.5. ספי לילה/יום
15.5.1. יום: שמים לא `#ffffff` על >30% מהפיקסלים העליונים (בדיקת histogram).  
15.5.2. לילה: luminance ממוצע של השליש העליון בין 8 ל-40 (לא שחור מוחלט, לא יום).

---

## 16. תקציב פריים — פירוק ms

יעד High דסקטופ 60Hz = 16.6ms. מדידה: `RenderTelemetry.last` + `renderer.info`.

16.1.1. גיאומטריה + forward: ≤7.0 — InstancedMesh, <80 draw calls איילון.  
16.1.2. CSM 2–3 cascades @1024: ≤2.0. אם חורג → 2 cascades.  
16.1.3. Planar 768: ≤1.5. חורג → 512 או כיבוי.  
16.1.4. SMAA + bloom חלש: ≤1.5.  
16.1.5. JS פיזיקה 120Hz: ≤3.0 (כבר; לא לגעת בלולאה בלי מדידה).  
16.1.6. רזרבה: 1.6.  
16.2. סדר כיבוי = 2.2.2 (planar → bloom → CSM → pixelScale).  
16.3. Low: 16.1.2=0, 16.1.3=0, 16.1.4=0, pixelScale≤0.75.  
16.4. אי אפשר להבטיח 16.6 בסנדבוקס headless (SwiftShader). שער CI: p95 < 40ms headless, <20ms אצל המשתמש.  
16.5. אסור לסמן "60fps" בלי מספר מ-`?qa=1` על המחשב של המשתמש.

---

## 17. SSGI / וולומטרי — רק אחרי 4.2 ו-6.5

17.1. Codex §25.10 ו-8.6: כבוי עכשיו = DONE כאילוץ.  
17.2. כשמותר:  
17.2.1. GTAO ב-half res, ≤2ms, High בלבד.  
17.2.2. לא SSGI מלא בסגנון Lumen.  
17.2.3. ערפל גובה (exponential) כבר קיים — לכוון, לא volumetric clouds.  
17.2.4. גשם: נקודות קיימות. לא ספלשים GPU.  
17.3. אם GTAO שובר p95 — למחוק, לא "להשאיר כבוי ב-High".

---

## 18. מצלמות Golden קבועות

18.1. קובץ קיים `src/world/goldenCameras.ts` — להרחיב, לא להחליף.  
18.2. כל pose: `{ id, trackId, x, y, z, yaw, pitch, tod }`.  
18.3. איילון: g01, g07, g08, hashalom.  
18.4. אחרי freeze: pose אחד לכל 7.x (rothschild-ficus, reading-stack, jaffa-clock, jaffa-gate, ramon-rim, hermon-snow, carmel-shrine).  
18.5. הרכב לא זז בזמן הצילום (`throttle=0`, `skipCountdown`).

---

## 19. מיפוי קודקס 26 סעיפים → העץ הזה

19.1. §1 שכבות Ultra/Balanced/Compat/Photo → 8.7.  
19.2. §25.1 hero → 4.2.  
19.3. §25.2 כביש אמיתי → **לא בסנדבוקס**; תחליף = 3 + 6.  
19.4. §25.3 תאורה → ACES קיים; HDRI = NOT DONE עד PNG EXR חיצוני (אין בסנדבוקס).  
19.5. §25.4 glTF/KTX2/Meshopt → 9.1 + 9.3.  
19.6. §25.5 cells → 9.4 אחרי 6.5.  
19.7. §25.6 כביש רטוב → 3.3.  
19.8. §25.7 WebGPU+TSL → 8.2–8.3.  
19.9. §25.8 CSM+probes → 8.4 + probe 96px קיים.  
19.10. §25.9 TRAA → 8.5.3 WebGPU only.  
19.11. §25.10 SSGI → 17.  
19.12. G0–G6 → 2, 3, 4.3, 8.  
19.13. P4 ישראל → 6 + 7.  
19.14. Photo mode → 8.7.4 (DPR, לא renderer שני).

---

## 20. HDRI — החלטה סופית בלי לחכות

20.1. אין קובץ `.hdr`/`.exr` ב-repo.  
20.2. אסור להמציא "HDRI" מ-canvas גרדיאנט (זה FAKED).  
20.3. אם המשתמש מספק `.hdr` CC0 (למשל Poly Haven) — PMREM ב-High בלבד, חשיפה 0.6–0.8, `metalness` כביש נשאר 0.  
20.4. בלי קובץ: שמים = `sky-day.png` / `sky-night.png` הקיימים. לא `Sky()` הפרוצדורלי כברירת מחדל אם הוא שוטף ללבן.

---

## 21. רשימת תתי-משימות לביצוע אחרי אישור (מספר אחד לסשן)

21.1. 1.1.1.2 משפט "בהשראה" על כל כרטיס.  
21.2. 1.2.2 hook מת בבילד פרוד.  
21.3. 4.5.2 HUD `kinMix`.  
21.4. 3.6.2 `import()` ל-NYC.  
21.5. 2.1.2 draw calls ב-HUD.  
21.6. 2.2.2 סדר כיבוי.  
21.7. 3.1.1 שרשור שיידר↔CSM + 3.2 UV דאשים בשיידר (מחליף מקפי mesh בהדרגה).  
21.8. 6.4.5 טסט Y על רמפת השלום.  
21.9. **עצירה.** 6.1 המשתמש.  
21.10. 6.2.4–6.2.6 פסי קומות 187/169/154.  
21.11. 6.5.1–6.5.2 hash הקפאה.  
21.12. 14.9.2 טסט היסט סמלים.  
21.13. 7.1 … 7.7 לפי הסדר, כל אחד + 15.4 pose.  
21.14. 8.2.1 `?webgpu=1`.  
21.15. 9.1.3 KTX2 או הסרת loader.  
21.16. 15.2 pixelmatch.

הפרת 21.13 לפני 21.11 = הפרת 0.3.

---

## 22. מה עדיין חסר במסמך (חלק ג׳ ב-`next` אם צריך)

22.1. טבלת 0–100 יעד לכל 5 דגמים (מספרי קטלוג) — בלי דגם מורשה אלה יעדים פנימיים בלבד.  
22.2. רשימת draw-call לאיילון (budget 80) — דורש מדידת `info.render.calls` חיה, לא ניחוש.  
22.3. רישיון hero glTF ספציפי — לא לבחור קובץ בלי בדיקת license.  
22.4. קובץ `.hdr` — המשתמש או NOT DONE.

אלה **לא** חוסמים את אישור חלק א׳+ב׳. חוסם יחיד למימוש שלב 7+: צילום השלום (6.1 / 13.5).

---

**סוף חלק ב׳.** תכנות עדיין אסור עד שתכתוב שהתוכנית אושרה.

