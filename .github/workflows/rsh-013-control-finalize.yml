name: rsh-013-control-finalize

on:
  push:
    branches:
      - agent/rsh-013-canonical-track-schema
    paths:
      - .github/workflows/rsh-013-control-finalize.yml

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/rsh-013-canonical-track-schema
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - name: Reconcile canonical post-RSH-013 state
        run: node scripts/finalize-rsh-013-control.mjs
      - name: Commit canonical transition
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && { echo "No transition changes"; exit 1; }
          git commit -m "RSH-013: reconcile canonical transition to RSH-014"
          git push origin HEAD:agent/rsh-013-canonical-track-schema
